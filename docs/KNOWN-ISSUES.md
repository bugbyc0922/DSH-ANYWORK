# 已知问题清单（P4-22）

> 记录日期：2026-09-19。按"现状 / 影响 / 处置"记录；清理后请更新本文件。

1. **端口转发自动刷新任务待一次 UAC 授权**——`C:\Users\Yangc\AppData\Local\desk-anywork\desk-task-setup.ps1`（管理员运行一次即建好登录任务）。
   影响：若 WSL 的 IP 在重启后变化，局域网（手机）访问需手工刷新 portproxy。当前 IP 稳定、Windows 本机访问不受影响。
2. **手机端最终确认未做**（PLAN P0-4 的 🟡）——需要一台同 Wi-Fi 手机打开 `http://192.168.0.171:8080` 登录一次。
3. **演示通道 `fake` 指向本机测试桩（:8199）**——重启后桩不会自启；不使用该模型名则无影响。可在门户「模型通道」里删除，或 `node tests/fake-oai.mjs` 重新拉起。
4. **演示账号密码为试用值**（alice / bob / boss）——正式把入口给同事前，用 `node src/cli.ts user passwd` 更换。
5. **隔离为"同一 OS 账号下的目录级 + 进程级"**——适合 1–3 人；强隔离（独立系统用户 / 容器 / 独立机器）为后续升级路径。
6. **流式响应缺失 usage 时按粗估记账**（`estimated=1` 标记）；账本口径以网关为准。
7. **知识库 v1**：无权限过滤、无自动更新（手动放入即生效）；**公司盘 v1**：界面无删除 / 移动、单文件 50MB 上限；**团队技能库 v1**：全员共享、无权限过滤（技能只是给 agent 的指引，实例权限档位不放宽）。
8. **多上游通道 v1**：仅支持 key 型 OpenAI 兼容通道；价目表为手填的本地估值（非厂商账单）。
9. **dsh 处于 rc 阶段**——已钉基线 0.1.0-rc.5；升级 dsh 后跑回归（多实例、信任围栏、插件机制、网关截获、RPC 载荷）。
10. **Windows 侧 git 配了 127.0.0.1:10808 代理**——代理离线时 Windows git 失败；推拉一律在 WSL 做。
11. **备份为本地目录**（`~/desk-backups`，保留 14 份）——无异地副本；重要节点可自行拷走。
12. **从零重装验收未做**（PLAN P4-20 绿线的完整版）——脚本与文档已就绪（`scripts/setup.sh` 幂等自检通过），待第二台机器或空闲时实际走一遍。
13. **WSL 空闲自动停机**已通过 `C:\Users\Yangc\.wslconfig` 关闭——若某次系统/WSL 大版本升级后服务又"莫名全死"，先查该文件是否被重置。
14. **团队 Agent 预设为存量拷贝**（基于 dsh `standard`，2026-09-19 版）——dsh 升级后建议按 `presets-template/README.md` 的步骤重抄（内置模式有变化时团队预设不会自动跟随）。
15. **会话检索为 opt-in 开启（非默认）**——`tool-session-query` 以 `link:` 依赖挂进实例 profile（指向 dsh 检出内包）；**dsh 检出路径变化时需重跑** `scripts/enable-session-search.sh`。索引落盘在各实例 `search.db`（首次搜索时构建）。
