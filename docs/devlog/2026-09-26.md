# 2026-09-26 · 日常

## 今天

- 今日 11 个提交（三条线：模型与通道 UI、成员管理自动化、部署与构建）。
- 模型与通道：团队 Key 面板与模型通道管理迁入「设置 → 模型」页脚官方槽位（管理员可改 / 成员只读）；通道一键添加（qwen / kimi / glm / siliconflow / openrouter 预设 + 连接测试）；通道模型自动同步进各实例模型选择器（settings.yaml 同步 + 通道变更后实例自动重载）；旧「管理员钥匙桥」方案退役。
- 成员管理全自动化：新建成员建号即自动配实例（钥匙 / 端口 / 插件，约 1 分钟可用；虚拟钥匙不再需要人工交付）；删除成员自动回收工作台（停实例 / 清数据 / 按 pid + DSH_HOME 精确杀进程）。
- 部署与构建：Docker 构建支持大陆网络（`DEBIAN_MIRROR` / `NPM_REGISTRY` 可选参数 + `build-cn.sh` 一键脚本）；修复 better-sidebar 过时探针（0.21+ 已不依赖 node-pty，旧探针曾致容器无限重启）。
- 文档同步：GUIDE 补团队 Key 行为说明；成员管理文档随自动化流程更新。

## 明天

- 手机端最终确认（P0-4 遗留）：成员手机实机再过一遍。
- 从零重装验收（P4-20 遗留）：第二台机器实走 `scripts/setup.sh` → 服务 → 门户全链路。
- 顺手项（人在机器前时）：管理员运行 `desk-task-setup.ps1` 完成 portproxy 刷新任务的 UAC 一次性授权（KNOWN-ISSUES #1）。
- 下一期候选（PLAN 待列，择一）：CI 工作流（「就位进度」唯一 ⬜）／花名册·同事视图／任务卡 + 提交验收四格／多通道余项（订阅式通道 / 价目表同步 / 余额告警）。
- 巡逻确认：状态正常（HEAD 5a15840）
