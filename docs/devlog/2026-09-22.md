# 2026-09-22 · 日常

## 今天

- 玻璃主题（Aqua）：安装社区插件 `dsh-client-ui-aqua@1.3.1`（WYH66666666/DSH-Transparent-UI-Plugin，406★）到三个实例——设置 → 插件 → 「玻璃主题」卡片可开关（每人本地偏好）；引入前定向审查干净（零网络 / 零 eval / 零 cookie，纯 theme token 覆盖）；版本对表依据 = 1.3.x peerDeps 钉 `^0.1.0-rc.5` 与本机同代。安装脚本 `scripts/install-transparent-ui.sh`（幂等）。提交 79bd918。
- 会话删除（核心功能）：「归档」之外新增真删除，且带权限流 ——
  - 机制：dsh 无 `session.delete` RPC；删除 = 停实例（`kill -9` MainPID，systemd `Restart=always` + `RestartSec=5` 提供编辑窗口）→ 删 `sessions/<bucket>/<sessionId>/` 目录 → 按会话 ID 过滤注册表 `storages/workspace.json`（`tables.workspaces.*.sessionIds` + `global.archivedSessionIds`，tmp+rename 原子写）→ systemd 自拉重启重读。新模块 `src/session-mgr.ts`（`listUserSessions` 走实例 RPC 环回、`deleteSession` 实施删除）。
  - 权限：仅管理员可直删；成员提交「删除申请」→ 管理员批准后由服务端执行，可驳回 / 成员可撤销。新表 `session_del_requests`；接口：成员 `/portal/api/session-mgr/{mine, request, cancel}`、管理员 `/portal/api/admin/session-mgr{,/decide,/list,/delete}`；申请时经通知桥推管理员（微信）。
  - UI：设置 → **会话管理**（side order 58，全员可见）——「我的会话」列表（经 `session.list` 实时拉取，含运行中/子会话标记）、申请删除 / 撤销 / 状态chip；管理员额外有「待审批（批准删除 / 驳回）」「成员会话（选成员 → 加载 → 直删）」「最近处理」。删除执行时对应实例约 5~10 秒后自动重启。
  - 实测：接口层全流程（申请 / 重复拦截 / 非法 ID / 撤销 / 越权 403 / 批准执行 / 直删）；文件 0 残留、注册表除名（含"非空注册表下兄弟 ID 幸存"专项）；浏览器管理员视角端到端（页面渲染 → 直删 → 刷新复核 → 审批）全绿。
- 认知修正：会话列表实际是**扫盘**列举（`sessions/` 目录），注册表只负责"工作区归组"；故实例注册表为空也不影响列表显示。

- 文档化：《功能使用指南》docs/GUIDE.md 上线——覆盖全部功能（设置 8 页 / 侧栏 4 入口 / 团队能力 / 通知通道开通步骤 / 钥匙体系 / 运维速查 + 功能索引总表，每个功能「入口→怎么用→注意」），README 双语互链；仓库全量同步远端已核对。

- 引擎大升级：dsh `0.1.0-rc.5 → 0.1.5-rc.2`（为社区插件而升；全程可回滚，快照在 `~/desk-backups/`）——
  - 构建：tag checkout + `pnpm install` + `pnpm clean` + `pnpm build`（清掉旧 tsbuildinfo，否则 tsdown MISSING_EXPORT 构建失败）。
  - 兼容修复（升级暴露、逐个修掉）：① 重复 loader entry（subagent-codex 由包自带 patch，删掉我们的手工插行）；② 凭据文档新格式（`version:1` + `refs:/records:`，引擎自动迁移 + `agent-run.sh` 补种检查兼容缩进）；③ **浏览器会话认证**（实例全面门禁：页面/API 需 authority 绑定签名 cookie —— 门户 desk-server 铸造并注入上游请求，新增 `src/instance-auth.ts`）；④ RPC 两段式协议（`/api/session/list` + `{args:{…}}`，session-mgr / desk-panel 双协议自动回退）；⑤ 运维探活把 401 视为在服。
  - 新装插件（三实例）：**better-sidebar**（右侧工作台：文件/终端/Git 面板）、**chat-import**（侧栏「导入会话」）；**free-web-search 放弃**（其依赖的 `installSettingsSection` 导出只存在于 rc.6~0.1.2 窗口，新旧引擎都不兼容）；玻璃主题（1.3.1 仅配 rc.5）暂移除待适配 fork。
  - 回归：三实例新引擎启动零错误；门户链路（登录/反代/会话列表/用量/会话管理页）；alice 试点 UI（设置 8 页 + 侧栏 + 导入会话入口）全绿。
  - 待办：官方 `web-search-deepseek`（需网关加 Anthropic 端点转发+记账）；玻璃主题 fork 适配。

- 玻璃主题适配版装回（升 0.1.5 后的收尾）：换用社区 fork `Zagadka-3906/DSH-Transparent-UI-Plugin-dsh015`（v1.3.1-dsh015.1，peerDeps 钉 0.1.5-rc.1；相对上游三处改动 = store 包迁移 / inject 清单换 ui-theme·locale·ui-settings / 总开关挪到通用设置）——
  - 审查：零网络零子进程、依赖仅 react + ui-primitives + client-store，host 半为空壳，通过。
  - 试点→全量：alice 验证（总开关关=glass 属性移除、开=玻璃回归；设置卡片+全套旋钮渲染正常）→ boss / bob 部署，三实例重启零错误、boot HTML 均引到 `dsh-client-ui-aqua/client.js`。
  - 说明书已更新：GUIDE §6.1 开关位置改为「设置 → 通用设置」（0.1.5 的「插件配置」页不再渲染无 key 第三方卡片）。

- 任务板升级：**提交·验收闭环**（人机可分辨的评审流程）——
  - 任务卡新增「提交验收」（提交内容 / 提交说明 / 可关联一条 DSH 会话=AI 参与标记）、创建者/管理员「通过 / 打回（必填理由）」、提交人「撤回」；新增「待验收」筛选；提交推创建者/管理员、评审推提交人（通知桥）。
  - 后端：tasks 表 +9 评审列（幂等迁移）+ /portal/api/tasks/{submit,review}；前端 desk-panel 任务板升级（会话选择器双协议）。E2E（alice×boss）：建→交→拦重复/拦越权→打回→重交→通过→撤回 全绿。
  - 规范落地：工作区 README 模板新增「提交署名（AI 提交加 Generated-by 尾注）」与「项目仓库 AGENTS.md 约定」；apply-soul.sh 修复为「更新时保留成员自定义尾区」；技能库 README 补登记 codex-parallel-worktrees。
  - 备注：E2E 期间微信通道遇 iLink 会话失效（外部问题，另行处理）；通知桥 notify_log 记录完整。

- 品牌换新：工作台**侧栏标题**与**欢迎页大字**改为 **ANYWORK-ToKenAI**（原「DSH 本地构建 / 探索未至之境」），并删除「预览版」徽章与**侧栏版本号小字**——改 dsh 客户端包源码（`locale` + `ui-conversation` + `ui-sidebar`），tsdown 增量重建后三实例重启生效；补丁脚本存 `patches/dsh-branding-anywork-tokenai.py`，**升级 dsh 后重放**。

- 部署升级：新增 **Docker 一条命令部署**（`docker compose up -d --build`，Linux 服务器通用）——
  - 形态：单容器 all-in-one（门户 + 网关 + 各成员实例；环回架构原样成立、零重构）；`/data` 卷存全部数据；首启幂等 bootstrap（建号/虚拟钥匙/端口/凭据/共享区/实例路由/插件）；容器内轻量监督（实例被杀 5 秒自拉，systemd 同语义）。
  - 运行时适配：`session-mgr` 增加 pidfile 兜底（无 systemd 也能"停实例→删文件→自拉"）、`ops` 面板无 systemd 按 pidfile 显示存活、`apply-soul` glob 兼容 `<用户名>` 家目录、`agent-run.sh` 支持 `DESK_TRUSTED_HOSTS` 多信任地址。
  - 实测（本机 Docker Desktop/WSL2，真实全链路）：build ✓ → 空卷首启 bootstrap ✓ → 登录 ✓ → 建会话/模型经网关/账本入账（n=2, 11046 tok）✓ → 会话删除（pidfile 杀 79→214、5 秒自拉）✓ → down+up 持久（登录 + 会话都在）✓ → boot HTML 四插件 bundle 全在 ✓。
  - 修复一个关键坑：compose 把真 key 传进容器后，dsh 环境层会拿它**直连上游、绕过网关（不计账）**——entrypoint 在 bootstrap 后 `unset DEEPSEEK_API_KEY`（服务进程继续从 `~/.desk/keys.env` 读），并给每个实例补 `settings.yaml` 的 `llm-deepseek.baseURL` 指向网关。
  - 两个"装不上"教训：运行镜像缺 `git`（github 源插件失败）与 C++ 工具链（node-pty 编译失败）→ 已补 `git make g++`；bootstrap 插件校验加"完整性探针"，失败重启容器自动重试。

## 全量 English 化（i18n）
- **desk-panel 接入官方语言机制**：注册 `ctx.locale.register("desk-panel", {zh, en})`（320 条 × 2 语言），全部界面文案走 `tr()`；中文兜底、英文精译；切语言即时生效、双向（实测 zh⇄en 逐页对照，审计 320/320 零缺失零冗余）。
- **门户双语**：登录页 / 我的用量 / 用量悬浮胶囊 / 管理页 / 全部 API 错误串 / manifest 按语言渲染（cookie `desk_lang` > 实例设置 > 浏览器语言）；门户注入 `window.__DESK_LANG`（悬浮组件用）。
- **服务端按语言输出**：`ops.ts` 的「门户 / 网关」端口标签、`panel.ts` 连接器 8 条文案与技能读取错误、上游来源「未配置」——客户端随当前语言上报 `?lang=`。
- **修好一个上游限制（关键）**：dsh 0.1.5 的 ui-settings 在非 loopback 页面（= 经门户 / LAN 访问）把设置降级为**内存模式**——语言、外观等设置不读也不写 `settings.yaml`、刷新即回退。新增 `patches/dsh-settings-lan-persistence.py`：门户注入 `window.__DSH_HOST_PERSISTENCE__=true`（门户=本部署信任边界），两个消费点放行 host 持久化；重建 `ui-settings` / `ui-settings-general` 两个客户端包。**实测：切中文 → 刷新保持 → 切回英文 → 刷新保持；换浏览器仍保持（写入服务器）。**
- **aqua fork 启动报错修复**：0.1.5 起 `settings.plugin.item` 为 keyed slot，fork 注册缺 `key` → 启动 Uncaught Error；补 `key: "ui-aqua"`（3 实例 + `~/opt/thirdparty/aqua-vet` 发行 tgz 同步；补丁记录见 `patches/aqua-plugin-card-key.py`）。
- **配套修**：语言信号钩子（面板 / 运维数据随语言重取）；全部 POST 助手带 `lang`；全角标点残留 31 处（括号、冒号、问号）。
- 验收：浏览器 console 零报错；四侧栏入口（助理 / 技能·连接器 / 自动化 / 公告板）双语正常；任务板中文回归通过。

## 明天

- 手机端最终确认（P0-4 遗留）：同一 Wi-Fi 手机开 `http://192.168.0.171:8080` 登录一次、进工作台看 UI。
- 从零重装验收（P4-20 遗留）：第二台机器实走 `scripts/setup.sh` → 服务 → 门户全链路。
- 顺手项（人在机器前时）：管理员运行 `desk-task-setup.ps1` 完成 portproxy 刷新任务的 UAC 一次性授权（KNOWN-ISSUES #1）。
- 下一期候选：CI 工作流 / 花名册·同事视图 / 任务卡 + 提交验收四格 / 多上游通道深化（订阅式通道 / 价目表同步 / 余额告警）。

## 巡逻

- 待 21:30 巡逻任务确认（HEAD 以当日提交为准）。
- 巡逻确认：状态正常（HEAD a6c6895）
