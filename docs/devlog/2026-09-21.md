# 2026-09-21 · 日常

## 今天

- 高产日：全天 19 个提交，团队协作层集中补齐（各项目均实机验证；明细随提交追记于 2026-09-19.md）——
  - 治理与入口：团队 Soul + 工作区 README（与 Hermes 主 agent 同源，`apply-soul.sh` 幂等铺装）；管理功能迁入工作台设置（desk-panel v4：成员 / 通道管理 + CLI `user rm`，外键安全删）；本机 dsh 入口切换为团队版（启动器替换）；官方「后台任务 + 子代理」真机验证并入工作区规范。
  - 协作面：通知桥 v1（webhook / Hermes 微信，实测真实到达）；定时提醒（服务端 30s 巡查，微信准点）；预算告警（80% / 100% 每档每月一次推管理员）；团队公告板 + 意见反馈；任务板（建 / 接领 / 状态流转 / 删除 + 权限圈）；运维面板（管理员专属）。
  - 安全：团队安全闸门（官方 hooks 桥 + 20 条危险命令策略：deny 拦截并推微信 / ask 弹审批；危险探针实测被拦）。
  - 能力面：知识库沉淀（笔记 API + `desk-kb` CLI + 设置页表单）；Codex 接入（`subagent_codex` 单发 + workflow 两路并行实测）+ `codex-parallel-worktrees` 共享技能；桌面端（PWA + Windows 桌面资产）；侧栏模块化入口（助理 / 技能·连接器 / 自动化）。
  - 其他：通知通道重试升级为阶梯（+45s / +3m / +10m，应对上游 iLink 限流）；报价换算页上线后当日按用户要求撤除（口径沉淀 KB 笔记）；演示账号误删事故以备份外科恢复，跟进项记入 KNOWN-ISSUES。
- 巡逻：`git pull` 已最新、工作区干净（本地 = origin/main）。

## 明天

- 手机端最终确认（P0-4 遗留）：同一 Wi-Fi 手机开 `http://192.168.0.171:8080` 登录一次、进工作台看 UI。
- 从零重装验收（P4-20 遗留）：第二台机器实走 `scripts/setup.sh` → 服务 → 门户全链路。
- 下一期候选起步（择一）：CI 工作流（「就位进度」唯一 ⬜）／花名册·同事视图／任务卡 + 提交验收四格／多上游通道深化（订阅式通道 / 价目表同步 / 余额告警）。
- 顺手项（人在机器前时）：管理员运行 `desk-task-setup.ps1` 完成 portproxy 刷新任务的 UAC 一次性授权（KNOWN-ISSUES #1）；跟进「删成员不回收实例 / 工作区」收尾。
- 巡逻确认：状态正常（HEAD 6834bbe）
