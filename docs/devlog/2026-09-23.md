# 2026-09-23 · 日常

## 今天

- 移动端适配冲刺（今日 18 个提交，门户 + 侧栏面板）：设置页全屏 + 顶部横滑导航（v1）→ 修设置页无法滚动与手机端遮挡（v2）→ 修导航芯片标签溢出重叠（v3）；四个侧栏面板改底部抽屉形态（触控 44px+、安全区、点面板外/✕ 关闭、轻压暗）；主界面微调（composer 触控按钮、输入字号 16、聊天中用量钮上移防遮挡）。
- 三个非显然坑及修法：① aqua 主题 backdrop-filter 让固定面板被锁进 54px 包含块（且桌面端 ✕ 被内容层盖住点不到）→ 面板打开时移除侧栏模糊（全平台修复）；② 老内核丢弃 `:has` 规则导致窄竖条破版 → JS 兜底挂 `body.desk-panel-open`；③ hero 输入框自动聚焦触发 `:focus` 隐藏规则误藏用量钮 → 焦点规则联锁「聊天态」。
- 手机安装与启动体验：补 192/512 PNG 图标（安卓 WebAPK 启动图正常渲染）；新增启动闪屏（慢网不再长时间黑屏，应用可用后淡出、兜底 60 秒）；GUIDE 补侧栏面板手机端使用说明。
- 预设修复：team-assistant persona 改用 dsh 新版 `prefix/suffix` 字段（旧 `text` 写法一切换即报错），`apply-team-persona.mjs` 兼容新旧两代。
- 部署与文档：deploy 提示语防呆（明确用 compose / deploy.bat、勿直接运行镜像）；KNOWN-ISSUES 补「容器版成员建号无实例」缺口与处置流程（byc/cth 实做经验）。

## 明天

- 手机端最终确认（P0-4 遗留）：成员手机实机再过一遍（今日已修老内核破版与安卓安装路径）。
- 从零重装验收（P4-20 遗留）：第二台机器实走 `scripts/setup.sh` → 服务 → 门户全链路。
- 顺手项（人在机器前时）：管理员运行 `desk-task-setup.ps1` 完成 portproxy 刷新任务的 UAC 一次性授权（KNOWN-ISSUES #1）。
- 下一期候选（PLAN 待列，择一）：CI 工作流（「就位进度」唯一 ⬜）／花名册·同事视图／任务卡 + 提交验收四格／多上游通道深化（订阅式通道 / 价目表同步 / 余额告警）。
- 巡逻确认：状态正常（HEAD 92d293f）
