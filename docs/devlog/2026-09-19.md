# 2026-09-19 · 项目起始日

- 清空 GitHub 账号旧内容：删除 8 个旧仓库（2020–2025 年的测试 / 模板仓库）。
- 建立公开仓库 **DSH-ANYWORK**，推入首版：README、MIT 协议、实施计划（docs/PLAN.md）、本开发日志。
- README 定为英文主版，中文版保留在 `README.zh.md`（顶部互链）。
- 技术底座确认：官方 dsh 提供全部所需口子（`DEEPSEEK_BASE_URL` / `DSH_HOME` / `--port` / `--trusted-host`），全程不改 dsh 源码。

## 下一步

- P0 可行性验证（dsh 多实例 / 启动参数 / 网关截获 / 局域网可达）。
- 挂每日巡逻任务（本目录每天一篇）。
- 巡逻确认：状态正常（HEAD 8ee1fce）

## 进度补充（当日完成）

- **P0 完成**（详见 docs/BASELINE.md）：dsh 多实例并存与隔离、信任围栏（拒绝侧 403 + 放行侧 200 闭环）、网关截获（含 `include_usage` 关键发现）、局域网通路（Windows 侧 200；手机侧待确认）。
- **P1 完成**：零依赖模型网关上线（虚拟钥匙 / 流式计量 / 账本 / 价格表 / 预算拦截 / CLI）；u3 实例实机接入验证——一轮发言 7653 tokens 入账，预算拦截 429 与恢复均实测通过。
- **P2 完成（12·13）**：门户（:8080）上线——登录 / 登出（内建 scrypt 密码散列 + httpOnly 会话 Cookie、错密码拒绝、5 次限速）、`/portal/me`（我的用量：本月 / 今日 / 最近请求，数字来自 P1 账本）、`/portal/admin`（成员管理 + 页面建号发钥匙；member 访问 403）。
  - 实测：未登录跳登录 ✅ · 错密码 err=1 ✅ · 对密码进门户 ✅ · member 访问管理页 403 ✅ · 建号（bob）后直接登录 ✅ · 登出会话失效 ✅。
- **P2 收尾（14·15 ✅）**：登录闸门 + 反代上线——登录后根路径直达本人 dsh 实例（Host/Origin 原样透传，实例以 `--trusted-host` 信任门户 authority）；`/api` WebSocket 升级透传。`scripts/start-agent.sh` 把实例启动自动化（自动带 `--trusted-host`）。
  - 实测：alice / boss / bob 三账号三实例完全隔离（各见各的会话）、WS 无 cookie 401 / 有 cookie 101、未登录 302、**局域网全链路**（Windows → 192.168.0.171:8080）同验通过。
- 提交轨迹：P0 基线 `17629ac` → P1 网关 `b984522` → 预算拦截 `a85f142` → 文档 `ba04d2e` → P2 门户 `1d1195c` → P2 反代。
- 体验修正（用户反馈）：登录后直接进工作台（原落点为「我的用量」中转页）；清理测试期残留会话（假网关回包、空会话）。
- 用量组件（用户要求）：反代向工作台 HTML 注入悬浮「📊 用量」面板（点开即见本月/今日/预算/最近 5 条；数据走新增的 `/portal/api/usage` JSON 接口），不再需要跳出门户页。
- **设置页插件打通（dsh 官方客户端插件机制）**：`plugin/desk-panel` —— 把「工作台用量」做进工作台「设置」导航（settings.section 槽位），**未改 dsh 源码、零构建、零 npm 依赖**；`dsh plugin --profile web add file:` 挂载（声明了 `dsh.bundle` 的包会被自动收进 profile 的 bundles 层）。u1/u2/u3 三实例全部挂载验证：设置里出现「工作台用量」，组件正常渲染，数据走 `/portal/api/usage`（经门户登录后可见真实账本）。
  - 踩坑①：`cordis.patch.yml` 的插入行必须写成 `- insert:` 块（顶层 `- id:` 会被当成"覆盖指令"，报 `entry not found`，静默不生效）。
  - 踩坑②：客户端 bundle 是闭包工厂格式 `window.__ModuleLoader__.load({ id, factory })`，`require` 只能引用平台模块（react / ui-slots / ui-primitives 等），导出 `apply` + `inject`。
- 参考对照（其他团队的构建）：公司台放进设置（同事 / 订阅 / 公司盘 / 知识库查询）——借鉴"设置内嵌"与信息呈现思路，未采用其代码；**公司知识库 / 公司盘 / 多上游通道管理已列入下一期候选**（见 PLAN）。
- **企业知识库 v1 上线**：共享目录 `~/desk-data/kb/`（仓库内 `kb-template/` 做模板，首启自动落位）+ 门户检索接口 `/portal/api/kb/search` + 设置页新增「知识库」查询页（关键词 → 命中行，带文件/行号）；每个实例 workspace 放了 AGENTS.md 提示（**会话里的 agent 可直接检索该目录**）。v1 为子串全文检索、全员共享、手动放入即生效；后续：权限过滤 / 自动更新 / 向量检索（暂不做）。
- **公司盘 v1 上线**：共享文件区 `~/desk-data/drive/`（仓库 `drive-template/` 做模板）+ 门户接口 `/portal/api/drive/{list,download,upload}`（路径围栏防逃逸、单文件上限 50MB）+ 设置页新增「公司盘」页（浏览文件夹 / 点文件下载 / 上传）；workspace AGENTS.md 追加提示。v1 全员可读写、无删除/移动；后续：删除/移动、权限、大文件分片。
- **多上游模型通道 v1 上线**：网关按**模型名**分流——外部通道（OpenAI 兼容）精确命中 → 用通道 base/key 转发；未命中 → 默认 DeepSeek。`channels` 表 + 门户「成员管理」新增**模型通道**卡片（加入 / 停用 / 删除）+ CLI `channel add/list/rm/on/off`；`/models` 合并下发（默认 + 全部启用通道，成员实例模型列表可见）；账本新增 `channel` 与 `cost_cny` 列（通道价目表 ¥/百万 tokens；缺价目表记 0），`eventCost()` 统一两种口径。测试桩 `tests/fake-oai.mjs`（假第二上游）：分流（流式 / 非流式）、通道计价、管理页表单增删、默认通道真实调用——全部实测通过。
- **P3 完成（实例管理 / 工作区规范 / 自启守护 / 隔离复查）**：systemd 化——`deploy/systemd/` 4 个单元（`desk-server` + `desk-agent-{boss,bob,alice}`，User=yangc、Restart=always），`scripts/install-services.sh` 一键安装启用；管理入口 `scripts/desk.sh status|start|stop|restart`；前台运行器 `scripts/agent-run.sh`（`start-agent.sh` 改调它）。**Windows 侧**：登录启动项（Startup vbs）拉起 WSL；`desk-net-refresh.ps1`（刷新 portproxy 到当前 WSL IP + 防火墙）与登录计划任务脚本（待 UAC 一次性授权）。**踩坑**：WSL 2.7 新增**空闲自动停机**（实例 ~15s / VM ~60s 两级计时器，闲置即杀服务）→ `C:\Users\Yangc\.wslconfig` 设 `vmIdleTimeout=-1` + `instanceIdleTimeout=-1` 解决；实测静默 130s 后实例仍 Running、localhost 与 LAN 均 302。**测试**：kill -9 实例 → 5 秒自拉；`wsl --shutdown` 真重启 → 四单元全自启、全端口 200；`check-isolation.sh` 全绿（密钥 600 / 目录 700 / 围栏 403·401 / 账本只存哈希）。
- **P4 主体完成（交付化）**：运维脚本 `setup.sh`（8 项幂等自检，全通过）/ `backup.sh`（VACUUM INTO 账本快照 + 数据 + 密钥 → 保留 14 份，`desk-backup.timer` 每日 03:40 已排程；实测 6.1K 归档齐全）/ `restore.sh`（先备份现状再还原）/ `logs.sh`；一页部署说明 `docs/DEPLOY.md`；问题清单 `docs/KNOWN-ISSUES.md`（13 项）。**试用验收实走**（见 `docs/ACCEPTANCE.md`）：bob 经门户真实链路 登录→工作台→建会话→发消息→**账本 ¥0.00796 入账、网关日志 usage=found**；alice 登录/身份/工作台全 200。遗留：从零重装（第二台机器）与手机端 UI 待确认。
- **团队技能库 v1 上线**：共享目录 `~/desk-data/skills`（仓库 `skills-template/` 首启落位）+ 每实例 `DSH_HOME/skills` 软链——走 dsh 官方 `skill-filesystem` 发现（`<dshHome>/skills` rank 400；默认跟随软链、带文件监听，改完即生效，无需重启）。示例技能 `team-report-style/`（报告写作规范）。
- **团队 Agent 预设 v1 上线**：共享目录 `~/desk-data/presets/`（仓库 `presets-template/` 首启落位；示例 `team-assistant` = `standard` 拷贝 + 团队版 persona，`scripts/apply-team-persona.mjs` 一键换 persona）+ 每实例 `DSH_HOME/.agent-presets` 软链（官方 user 预设根）。成员在 设置 → Agent 预设 可见可选，会话可用 `agentPreset: "team-assistant"` 指定。
- **会话导出 / 跨会话检索上线**：① 导出 = 官方 web 内置——会话头 `Session log` 按钮或 `/export` 命令 → 下载 ZIP（会话 + 子会话 + 附件）；实测 `GET /api/session.export` 返回合法 ZIP（30.5KB）。② 检索 = 官方 opt-in `tool-session-query`（agent 的 `session_search`/`session_event_search` 等工具；工作区授权，只搜同工作区）：`scripts/enable-session-search.sh`（link: 装依赖 + profile 补丁开索引 `openAt: first-search` 落盘 + 挂工具行）已铺三实例；实测 bob 的 agent 搜「验收测试」**精准命中历史会话**（报出 session id / 标题 / 命中 seq）。踩坑：profile 补丁原为空数组 `[]`，追加前必须先移除占位符（脚本已幂等处理）；link: 依赖靠 dsh 工作区 node_modules 解析（file: 会因 `workspace:^` 失败）。
- **团队 Soul + 工作区 README（与 Hermes 主 agent 同源）上线**：`soul-template/`（`SOUL.md` 行为准则 / `WORKSPACE-AGENTS.md` 工作区 README / 说明）+ `scripts/apply-soul.sh`（幂等）——soul 落共享区 `~/desk-data/soul/SOUL.md`，各实例 `<DSH_HOME>/AGENTS.md` **软链**过去（官方 agent-instructions 支持追随文件软链；`$DSH_HOME/AGENTS.md` 即"用户全局指令"，每会话注入）；工作区 README 同步到 `<实例>/workspace/AGENTS.md`。soul 文本与用户 Hermes agent 的 `SOUL.md` 同源——行为准则逐字保留，仅身份句按工作台语境调整。实测：新会话提问"回复长度怎么匹配问题分量"→ 历史里可见完整注入 + agent 答出准则（会话 `session-8aff755d…`，standard 预设同样生效）。
- **本机 dsh 入口切换为团队版**：停用原版 `dsh web`（3080，dev 模式）；桌面启动器由 `启动DeepSeekHarness.bat` 替换为 `启动DSH团队工作台.bat`（等待门户就绪后打开 `http://127.0.0.1:8080`；服务 systemd 常驻、关窗不影响）。旧家目录 `~/.dsh` 保留（KRouter 插件挂靠其中）。
- **管理功能迁入工作台设置 + 成员删除（补缺口）**：① 新增管理员 JSON API `/portal/api/admin/*`（overview / member-create / member-delete / channel-create / channel-toggle / channel-delete；仅 admin 会话）。② desk-panel 插件 v4 新增设置页「成员管理」：成员表（预算/实例/本月用量，右删；唯一管理员不给删钮显示"唯一管理员"）、新建成员（钥匙只显一次 + 复制按钮）、模型通道表（停用/删除）+ 加入通道表单。③ 旧 `/portal/admin` 页面下线（302 → 工作台），门户导航移除入口；④ CLI 新增 `user rm <username>`。⑤ 删除踩坑修复：`api_keys`/`usage_events`/`login_sessions` 三表对 `users(id)` 有外键——清删用 `PRAGMA foreign_keys=OFF` 包住（删钥匙行 + 会话行 + 用户行；**历史 usage_events 保留**作账本，孤儿行在报表里自然隐藏）。实测：API 全绿（建/删成员、非管理员 403、唯一管理员守卫、通道 CRUD）；浏览器 UI 真删走通（成功提示 + 名单刷新）；老页面 302、导航无残留。附注：`sudo` 转交互密码后，服务重启用「kill -9 MainPID → systemd `Restart=always` 自拉」替代（4 服务实测全自愈）。
- **通知桥 v1 上线（把消息推出工作台）**：官方 dsh 只会在浏览器里弹提醒——补上外推：① 设置页第 5 页「通知」（order 90）：通道列表（webhook / hermes 两类）、添加表单、发送测试、最近发送记录；② 管理员 API `/portal/api/admin/notify*`（routes 增删改 + notify-test）；③ agent 侧入口 `POST /portal/api/notify`（令牌认证，`~/desk-data/notify.token` 服务首启生成）+ 脚本 `~/desk-data/bin/desk-notify "标题" "正文"`（随 setup.sh 落位；工作区 AGENTS.md 已教 agent 何时用）；④ 存储：`notify_routes` + `notify_log` 两表。**实测**：mock webhook 收到 JSON（HTTP 200）+ **Hermes 微信真实到达**（两条测试消息）+ desk-notify CLI 双通道 ✓ + 错令牌 401 ✓ + 浏览器设置页渲染/开关全绿。hermes 通道 = WSL 直接调 Windows 侧 `hermes.exe send -t weixin`（interop 实测可行）。
- **定时提醒（到点自动推）上线**：服务器侧 30 秒巡查（`server.ts` 启动补发一次错过的 + `setInterval` 30s；不依赖浏览器/会话，`reminders` 表落库）→ 到点走通知桥推送。① agent 命令 `~/desk-data/bin/desk-remind "10:00" "内容"`（认 `明天 09:00` / `09-22 10:00` / `+30m` / `+2h`；`list` / `rm <id>` 管理）；② 令牌接口 `POST|GET /portal/api/remind`（+`/remind/rm`）；③ 设置页「通知」内新增"定时提醒"块（列表 + 删除）。**实测**：11:59 设定的提醒 → 12:00:26 微信准点到达（巡查粒度内）；+30m 条目建删 ✓；错令牌 401 ✓；UI 删除 ✓。后续可接：预算告警等事件源、重复提醒（每天）。
- **成员管理增强（预算告警 / 在线状态 / 7 天趋势）**：① **预算告警**——`maybeBudgetAlert()` 挂在网关 `record()` 尾部（异步不阻塞请求；`budget_alerts` 表 `UNIQUE(user_id,month,level)` 保证 80% / 100% 每档每月只发一次；金额用 `eventCost` 逐行算与界面同口径），到阈值经通知桥推管理员微信；与网关原有"超限 429"硬闸互补（100% 告警=最后一条能过的请求同时触发）。② **在线/最后登录**：overview 返回 `online`（有无未过期会话）+ `lastLogin`，成员行显示"● 活跃会话 / ○ 无会话 · 最后登录 …（从未）"。③ **7 天趋势**：成员管理页顶部小柱图（团队合计，按北京日聚合）+ 每人"近 7 天 ¥x"。实测：t-budget（预算 0.000001）发一次真请求 → `budget_alerts(100)` + **微信"预算提醒"到达 ok**；overview 新字段与浏览器渲染全绿。
- **团队公告板 + 意见反馈上线**：侧栏左下角新增 📢 入口（用 dsh 官方 `sidebar.footer.action` 槽位，常驻可见；有新公告显示未读数，点开即读清零）。面板＝公告列表（管理员发布/删除）+ 意见反馈（成员提交；管理员看全员并可删，成员只看自己的）。接口 `GET /portal/api/announcements`、`POST /portal/api/announcements/{post,rm}`、`POST /portal/api/feedback{,/rm}`（登录会话即可，管理操作 admin 守卫）；新增 `announcements` + `feedback` 两表。实测：后端权限矩阵（成员发公告 403、反馈隔离、测试数据清理）全绿；浏览器全流程（徽标「📢 公告（1）」→ 打开 → 界面发布 → 界面删除 → 关闭后徽标清零）。
- **运维面板上线**：设置页第 7 页「运维」（order 100，管理员专属）——服务状态（systemd 四个单元 ●/○ + pid + 启动时间）、端口探活（门户/网关/三实例）、备份（份数/合计/最后一份名+大小+时间）、磁盘/数据（空闲、desk.db、知识库、公司盘）、主机/进程（WSL 运行时长、内存、load、进程 uptime、node 版本）+ 刷新。数据源 `src/ops.ts`（`collectOps()`：systemctl / fetch 探活 / statfs / du，全部只读），接口 `GET /portal/api/admin/ops`（成员 403 实测）。踩坑：备份 mtime 直接切 ISO 会显示 UTC（差 8 小时）→ 增加本地时间字段 `mtimeLocal`；实例内插件文件与仓库副本是硬链接（`cp` 报 same file，无害——改仓库即全同步）。
- **任务板上线**：设置页新增「任务板」（order 55，紧跟用量）——团队轻量待办：任何人可建任务（标题/备注/指派下拉）、接领无主任务、状态一键流转（待办→进行中→已完成→重开）、删除（管理员或创建人）；筛选：全部/待办/进行中/已完成/我的（带计数）。接口 `GET /portal/api/tasks`（回 role/me/members/tasks）、`POST /portal/api/tasks/{create,update,delete}`；新增 `tasks` 表。权限：管理员/创建人/当前指派人可改，任何人可接领无主任务、创建人可删自己的。实测：后端权限矩阵全绿（空标题/瞎指派 400、动别人的 403、接领后即可推进）；浏览器全流程（UI 建 → 开始 → 完成 → 筛选 → 删）全绿。附：时间戳统一 `fmtAt()` 转浏览器本地时区显示（原 UTC 直切差 8 小时，公告板/反馈同步修正）。
- **官方「后台任务 + 子代理」接入团队（验证 + 规范）**：dsh 官方的 jobs（后台任务：`bash run_in_background` 提交 + `job_list/job_output/job_kill` 管理，会话头有官方任务条 ui-jobs）与子代理委派（`subagent`/`subagent_fork` + `send_message`/`interrupt_agent`/`list_agents` + 子代理目录 ui-subagent）本就随默认编排挂载（bundle/base 宿主编排 + bundle/web-app 客户端），**团队无需改码**。**真机实测**（bob 实例 RPC）：① `bash run_in_background: true` 跑 `sleep 8 && echo JOBBG-OK` → `job_list` → `job_output(job_id=bash-1, wait)` → agent 准确报告「bash-1 已跑完（exit 0），输出 JOBBG-OK」；② `subagent` 委派算术子任务 → 子代理答「21」→ 父代理转述。**团队层增量**：工作区 README（WORKSPACE-AGENTS.md）补用法规范（长任务走后台、重活派子代理），随 apply-soul.sh 铺到三实例。备注：`subagent_codex` / `subagent_claude_code` 官方默认禁用（需 WSL 装对应 CLI，留作未来可选项）。
- **团队安全闸门上线（官方 hooks 桥 + 危险命令策略）**：借 dsh 官方 `hooks-claude-code` 桥（PreToolUse 拦截点，deny>ask>allow 最严合并）给每个成员实例的 bash 工具加了一道**执行前闸门**：`hooks-template/`（hooks.json + danger-gate.mjs + rules.json + README）落共享区 `~/desk-data/hooks/`，`scripts/install-hooks.sh` 幂等铺装（link: 装桥 + 实例 profile 补丁挂行）。策略 20 条——11 条 deny（递归强删根/家目录/团队关键目录、mkfs/dd 写盘、删密钥·库文件、fork 炸弹、chmod -R 777 根、停用工作台服务、sudo rm -rf、关机重启、清 crontab/删账户、库破坏语句）；9 条 ask（sudo、curl|bash、force push、绝对路径递归删除、批量杀进程、写 /etc、清防火墙、递归 chown…，成员在会话里看到审批框）。**deny 时会经通知桥推管理员微信**（detached 不阻塞判定）；规则改 `~/desk-data/hooks/rules.json` 即生效（脚本每次调用重读）。实测（bob 实例 RPC）：危险探针 `rm -rf ~/desk-data/kb/__gate_test_nonexistent__` → 工具返回 `Error: 团队安全闸门拦截：递归强删团队关键目录…`（isError、命令未执行、目录无损）；`echo gate-allow-ok` 正常放行。配套说明：官方沙箱+审批栈本就兜底（workspace-write + ask，越界写弹审批、fail-closed），本次未动其默认。
- **通知桥加固（hermes 通道自动重试）**：安全闸门首推微信时遇到一次偶发失败（hermes.exe 非零退出，手动重放即成功）→ `sendViaHermes` 失败后 **45 秒后台重试一次**（`scheduleHermesRetry` 按日志行 id 挂 timer；成功回填 `notify_log ok=1 / info=ok（45s 重试成功）`，再失败则追加错误信息）。补丁后冒烟：desk-notify 全链路 `ok` 入库。
- **微信通道限流事件（排查记录）**：安全闸门首推与通知桥冒烟两次微信未达，根因在 Hermes 网关侧日志——`[Weixin] iLink sendmessage rate limited; cooldown active for 30.0s`（上游 iLink 限流，非本项目中继问题）。CLI 退出码语义：被限流**拒收**时非零（管道记『失败』）；已受理但异步失败时为 0（手动重放即此情况，同样未送达）。处置：hermes 通道重试从『一次 45s』升级为**阶梯重试（+45s / +3min / +10min）**，逐次回填 `notify_log`（成功置 `ok=1`，全败保留末次错误）。备注：12:16:22 的预算提醒是本通道至今最后一次成功送达；15:11 起待限流窗口解除后由重试链补送。
- **报价换算页上线**：设置页新增「报价换算」（order 45，置顶；**纯前端计算器、无后端**）——铬矿商务口径：吨度价(元/干吨度) × 品位% ＝ 元/干吨（**不除 100**）、折美元（÷汇率）、折湿吨单价；美元价反推吨度价；总货值 ＝ 干吨 × **未舍入单价**（湿吨自动扣水分）；汇率记 localStorage。实测（浏览器）：57.5 × 48.47 → 单价 2787.03（含浮点半进位修正——JS 原生 toFixed 会把 2787.025 舍成 2787.02）；1120 干吨 → ¥3,121,468.00（若先舍入单价会差 5.6 元，行家一眼看出）；837.9 湿吨 @2.75% → 814.858 干吨 → ¥2,271,028.92。工作区 README 同步口径提示（防 /100 老坑）。
- **知识库沉淀上线（候选第 10 项收官）**：共享知识库支持「沉淀笔记」——① 后端：`src/kb.ts` 新增 `saveKbNote/listKbNotes/readKbNote/removeKbNote`（`kb/notes/` 子目录、frontmatter title/author/time/tags、文件名=时间戳-标题 slug、`safeNoteName`+resolve 前缀双围栏、单条 ≤10 万字符、README 不进列表）；接口 `GET /portal/api/kb/{list,note}`、`POST /portal/api/kb/save`（**会话或 notify 令牌双通道**）、`POST /portal/api/kb/rm`（仅管理员会话）。② UI：设置→「知识库」底部新增「沉淀」块（标题/标签/正文 + 最近沉淀列表：查看展开、管理员删除）。③ Agent 侧：`~/desk-data/bin/desk-kb "标题" "正文"`（stdin 长文；`list` / `search 关键词`），工作区 README 已教用法。实测：会话/令牌/成员三路径 + 越权 `../` 双拒绝 + 成员 rm 403 + CLI 三命令全绿；浏览器 UI 沉淀种子笔记《铬矿报价口径速记（吨度价·干吨·湿吨）》（可查看、可搜、保留入库）。
- **报价换算页移除（用户要求）**：设置页「报价换算」当日下架——client.js 组件与注册删除；README（中英）/ PLAN / 工作区 README 同步去引用；KB 种子笔记《铬矿报价口径速记》**保留**（仅去掉指向该页的一行）。口径本身（吨度价×品位=元/干吨、未舍入单价、干吨结算）仍是团队常识，沉淀在知识库。
- **Codex 接入团队工作台（多线程并行工作流）**：① WSL 安装 `@openai/codex`（0.155.1，实测与 README 钉的 0.147.0 兼容）+ 登录态从 Windows `~/.codex/auth.json` 迁移（600）；② 每个实例 profile 补丁挂 host 行 `subagent-codex`（跑 `codex app-server --stdio`，无人值守）；③ `team-assistant` 预设启用 `tool-subagent-codex`（去掉 `disabled`）并把 **workflow 引擎 `provider: spawn → codex`**（`workflow` 工具的并行子任务改跑 Codex）；④ 三实例默认预设设为 `team-assistant`（`settings.yaml` `agent-presets.default`）。**实测**（bob，RPC）：`subagent_codex` 单发 → Codex 答 42 ✓；`workflow` 两路并行 → `tool-workflow/run-start` + `agent-start`×2 → 12×2=24、34×2=68 ✓。坑：codex provider **不支持 `outputSchema`**（结构化输出 schema 报 `does not support the "outputSchema" capability`，改纯文本返回即可）。注：Codex 消耗用户自己的 Codex 账号额度，不进工作台账本。
- **事故与恢复（演示账号被删）**：发现 alice/bob 两演示账号从用户表消失（大概率经「成员管理」界面删除——**删成员不会停实例**：实例仍在跑但虚拟钥匙失效 → 模型请求 401 `invalid or missing api key`）。处置：从当日 03:40 备份（`~/desk-backups/`）**外科恢复** `users` + `api_keys` 各两行（逐行 INSERT OR REPLACE，保留现库其它数据），网关用 bob 钥匙实测 200 恢复。跟进项已记 KNOWN-ISSUES（member-delete 不回收实例/工作区）。
- **Codex worktree 并行工作流（团队技能）**：把「多线程并行工作流」落成共享技能 `codex-parallel-worktrees`（技能库全员自动发现）——每任务一工作树 + 一条后台 `codex exec`，互不干扰；覆盖批量修 issue / 批量审 PR / 重构拆分 / 自动化作业。**真机实测**（本机 WSL）：两工作树并行 codex、文件各自落位 ✓；关键坑——worktree 的 git 元数据在主仓 `.git/worktrees/` 下，`workspace-write` 沙箱**挡写 index.lock**（codex 无法自己 commit）→ 两条正确路线均实测：① codex 只改文件、编排方收口 commit（推荐）；② `--sandbox danger-full-access` 让 codex 自己 commit。工作区 README 已指路。
- **桌面端（应用模式/PWA）上线**：① 门户挂 PWA——`/portal.webmanifest` + `/portal-icon.svg`（登录页 `page()` 与实例页注入标签 `USAGE_WIDGET_TAG` 均带 manifest/icon/theme-color；实测 200）。② `deploy/windows/` 桌面资产——`make-desktop-icon.py`（PIL 画 256 多尺寸 .ico/.png → `%LOCALAPPDATA%\desk-anywork\desk-app\`）、`make-shortcut.ps1`（Chrome `--app=` 独立窗口 + 专属图标）、`dsh-workbench-app.bat`（成员可转发）；本机桌面已建「DSH 团队工作台（应用）」快捷方式。③ 口径：壳加载的就是同一网页 → **功能永远与网页端同步、无需重新生成**；成员侧用转发 bat 或浏览器「安装应用/创建快捷方式（窗口）」。坑：**含中文的 .ps1 必须存 UTF-8 带 BOM**（否则 PowerShell 5.1 按 GBK 读→解析报错，本次实踩）。
- **侧栏模块化入口（WorkBuddy 式）**：侧栏 `sidebar.footer.action` 在公告旁新增「🧑💼 助理」（order 86）「🧩 技能·连接器」（87）「⚡ 自动化」（88）三个入口（设置上方；rail 态显示 emoji）。① 助理=共享预设一览（name/description/含 Codex 标记）；② 技能·连接器=技能库列表（点击看 SKILL.md 全文）+ 连接器状态（模型网关/通知/Codex/GitHub/知识库·公司盘，只读探测）；③ 自动化=定时提醒（成员可看列表；管理员表单增删，支持 `10:00`/`明天 09:00`/`09-22 10:00`/`+30m`）。服务端新增 `src/panel.ts` + `/portal/api/panel/{presets,skills,skill,connectors,auto}` + `/portal/api/admin/reminders/add`。实测：后端 8 项 + 浏览器全流程（四个入口/面板内容/技能全文/连接器/增删/关闭）全绿。
- **UI 打磨（用户反馈「太丑」）**：① 设计系统——client.js 注入 `DESK_CSS`（`.ddb` 触发行 / `.ddp` 面板外壳（图标瓷砖+标题+副标题+✕）/ `.tabs` 分段控件 / `.dot` 状态点 / `.badge`·`.tag` / `.inp`·`.btn`·`.mini` / `.ft` 提示），公告板与三面板全部重排版（逻辑零改动）；② **坑**：壳 `footerActions` 容器为 flex 横排 nowrap——四个带文字入口被挤成一行溢出（「自动化」截断、「公告」出屏）→ `[class*="footerActions"]{flex-direction:column!important;align-items:stretch!important;gap:2px!important}` + `>div{width:100%}` 覆写成纵向菜单（hash 类名取语义子串，dsh 升级后留意）；③ 门户 STYLE v2（字体栈 / 卡片 14px 圆角 / 蓝色主按钮 / 输入焦点环 / 粘性头部）+ 登录页品牌图标；④ 用量悬浮胶囊深色 → 浅色卡+柔和阴影；⑤ 视觉验收：browser_exec 截图 + vision_analyze（region 裁剪放大）迭代——侧栏菜单终评 9.5/10。
- **连接器扩展：Telegram + WhatsApp**：① notify.ts 新增两种通道 kind——`telegram`（Bot API，target=`bot_token|chat_id[|api_base]`，api_base 可指反代）与 `whatsapp`（provider 三选：`callmebot|apikey|手机号` / `greenapi|id|token|chatId` / `ultramsg|id|token|to`，均可加 `|base`）；addNotifyRoute 校验 + dispatchNotify 分派同步扩展。② 设置→通知 表单：kind 下拉 +2、按 kind 的占位符与提示、目标在列表中对 tg/wa 打码显示。③ 连接器面板新增「Telegram（连接器）」「WhatsApp（连接器）」两条（未配置给引导文案）。④ 回归：`tests/mock-connect.mjs` 假网关（:8092）实测 4 条路由 payload 全对、notify_log ok=1、清理无残留；**api.telegram.org 从本机直连可达**（假 token getMe 返 401 JSON）。
- **修复「Models 设置页无法填入 API 密钥」（用户报）**：根因 = 实例用**启动环境变量**传虚拟钥匙——credentials-local 四层里 env 只读且永久压过存储层 → describe 返回 `writable:false` → ProviderEditor 把输入框整体禁用（`keyLocked`）。修法：① `agent-run.sh` 不再 export `DEEPSEEK_API_KEY`，首启播种 `$DSH_HOME/.credentials.yaml`（0600）；② 新增 `scripts/seed-credentials.sh`（幂等；默认只补缺，`--force` 才覆盖自定义值）；③ 三实例 settings.yaml 补 `llm-deepseek.baseURL=http://127.0.0.1:8100`（Models 页可见真实网关）；④ `backup.sh` 打包 `.credentials.yaml`、`check-isolation.sh` 校验其 600。实测：describe×3 → `source:"file", writable:true`；UI 编辑卡输入框可键入（占位「已配置——输入新值可替换」）；**alice 全周期实测：UI 改钥匙→保存→落盘→还原**；bob/alice 经网关真跑回复正常。
- **团队共用真 key 进 UI（成员管理 →「团队共用上游（默认）」）**：显示 upstream 地址 + 打码 key（`sk-ec****2418`，来源 keys.env）+ 更换表单；后端 `/portal/api/admin/upstream{, -set}`（admin；写入 `~/.desk/keys.env` 0600；换 key 后先回响应再 `process.exit(0)` 由 systemd 自拉生效，约 5~10s；同值 unchanged 不重启；`DESK_REAL_KEY` env 覆盖时只读并提示）。实测全周期：打码读取 → 非法格式拦截 → 同值 unchanged → 假 key 更换+自动重启生效 → 还原 → bob 经网关真跑恢复 ✓。
- **玻璃主题插件（Aqua）上线**：社区插件 `dsh-client-ui-aqua@1.3.1`（repo WYH66666666/DSH-Transparent-UI-Plugin，MIT；peerDeps 钉 `^0.1.0-rc.5` 与本机同代，fork 们才是给新版 DSH 的）装进三实例 web profile。**引入前审查**：21 文件全扫——零网络请求（fetch/XHR/WS=0）、零 eval/atob、零 cookie 访问、唯一 URL 是 SVG 命名空间、字体内嵌 data:woff2；节点半是空 `apply(){}`。**开关位置** = 设置 → 插件 → 插件配置 →「玻璃主题」（浏览器本地偏好，每人各自开关；关掉完整还原原生 UI）。经门户路径实测（boss）：开启后深色玻璃主界面、侧栏/聊天卡片磨砂、我们的侧栏菜单（助理/技能·连接器/自动化/公告）完整显示，视觉评分 9.0/10 无破损。安装脚本 `scripts/install-transparent-ui.sh`（幂等；从 systemd 单元推导家目录）。
